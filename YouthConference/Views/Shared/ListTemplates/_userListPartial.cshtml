@model System.Collections.Generic.IEnumerable<YouthConference.Models.UserProfile>

<table class="table table-striped table-condensed">
    <thead>
        <tr>
            <th>User Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var i in Model)
        {
            <tr>@i.UserName</tr>
        }
    </tbody>
</table>